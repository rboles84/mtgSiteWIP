# VM-573 Archscry Runtime Decomposition Handoff

Agent: Codex

Task requested: Implement VM-573 as one task through Phase A Commander-dossier decomposition and Phase B Archscry-route decomposition, preserving behavior and compatibility, then stage the complete change for owner acceptance.

Related card: `docs/kanban/in-progress/VM-573-archscry-runtime-decomposition-pass-2.md`

## Files Reviewed

- Current governance: `CLAUDE.md`, `docs/dev/RobDevPass.md`, `docs/qa/RobQAPass.md`, current board/handoffs, VM-570/VM-571/VM-572 records.
- Archscry runtime, dossier domain, HTML entrypoint, shared session runtime, package scripts, focused tests, source/generated contracts, and current architecture/reference maps.

## Files Changed

- Compatibility facades: `assets/js/archscry/index.js`, `assets/js/archscry/commander-dossier.js`.
- Dossier owners: four modules under `assets/js/archscry/dossier/`.
- Route owners: twelve modules under `assets/js/archscry/runtime/`.
- Direct owner import: `assets/js/archscry/archscry-presentation.js`.
- QA/source ownership: frontend lint/smoke/copy checks, focused VM-551 scripts, Archscry/deck-link/placement tests, and `scripts/lib/read-archscry-runtime-source.mjs`.
- Current operational docs: project atlas, data-flow map, core logic, data contracts, method reference, board, and VM-573 card.

## What Changed

- Phase A extracted the Commander dossier implementation once into pure foundation, reading, precon, and audit owners. The 40-export facade and `archscry-result.js` compatibility contract remain intact.
- Phase B extracted route state, data, navigation, questionnaire, interview, dossier rendering/controls, content, card media, render helpers, actions, and boot behavior once. `index.js` remains the HTML entry, 21-export facade, 16-global facade, and explicit lifecycle owner.
- `APP_STATE` remains one object. Boot order, route listeners, query/storage/history behavior, and module-relative canonical data resolution are preserved.
- Source-inspection tests now follow explicit owners without changing product assertions.

## Why It Changed

VM-570 established product ownership but deliberately left large active Archscry files intact. VM-573 creates cohesive ownership boundaries without a framework, behavior change, state redesign, or facade break.

## Decisions Made

- Extracted modules may depend on lower-level owners, never back through compatibility facades.
- Facade/global parity is certified statically against baseline source without executing route initialization for introspection.
- Historical VM producers and frozen provenance references were not mechanically rewritten.
- Existing source-contract failures remain recorded instead of being weakened or repaired in this architecture pass.

## Risks / Uncertainties

- Local `node_modules` lacks declared/locked `xlsx` and `chrome-launcher`. The full suite and scripted rendered checks stop at those environmental boundaries.
- The in-app rendered fallback proved route/data/dossier/Maze-path loading but did not complete a retake journey after browser control became unavailable. Owner acceptance retains the short interactive check.
- Large cohesive owners remain in `dossier/foundation.js` and `runtime/dossier-view.js`; they were not split further where a clean acyclic boundary was not established.

## Tests Run

- PASS: static facade/export/global/boot parity; 32-module path and cycle scan; no facade back-dependencies; canonical `factions.json` resolution.
- PASS: JS lint (27), copy boundaries (30), frontend smoke, placement (37/37), Gate B1 questionnaire/runtime/result contracts, 5,000 result journeys, 2,000 recovery journeys, deck links, discovery education, presentation snapshots (16), adjacent navigation, Scryfall request deduplication, card-rationale authority, provider/Maze parity (155/155 destinations; 147 paths), source/generated guardrails.
- BASELINE-LIMITED: dossier integrity, dossier follow-up, Gate A source contract, VM-559 media projection, VM-572-relative dossier audit.
- FULL GATE: run once; passed all preceding suites and stopped at locally absent `xlsx`, matching baseline.
- RENDERED: scripted browser smoke/visual comparison stopped at locally absent `chrome-launcher`; fallback local route/dossier rendering passed with no faction/data error and no tracked output drift.
- PASS: `git diff --check`; zero product/data/generated/snapshot/audit/research/frozen-evidence drift.

## RobDev / RobQA Readiness

- Changed behavior: code ownership only.
- Protected behavior: exports, globals, `APP_STATE`, boot order, placement, dossier output, navigation, persistence, Maze handoff, data authority, and generated bytes.
- QA tier: high architecture risk with deterministic focused coverage, one full gate, and bounded rendered evidence.
- Owner acceptance: one quick reading, adjacent fit, one panel/card interaction, Maze handoff, and mobile sanity check.

## Not Touched

No product copy, HTML, CSS, data, scoring, placement meaning, generated artifact, dependency, Node/toolchain, framework, dead-tooling, EOL, or later architecture-pass work.

## Follow-Up Recommendations

VM-573 owner acceptance passed and the implementation is committed at `acca599ee3b9b8be2a1d7324ac04f5cd03c72f6b`. The Silverquill Card Signals finding predates VM-573 and remains a separate governed-content planning candidate; no follow-up task was created during closeout. Do not begin another architecture pass from this handoff.

Next suggested agent: None for VM-573; task complete.

## Closeout

- Owner acceptance: PASS, 2026-08-20.
- Implementation SHA: `acca599ee3b9b8be2a1d7324ac04f5cd03c72f6b`.
- Product/data/media changes during closeout: none.
- Card Signals follow-up opened: no.
