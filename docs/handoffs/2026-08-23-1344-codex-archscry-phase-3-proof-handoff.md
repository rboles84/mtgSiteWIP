# Archscry Phase 3 Proof Handoff

- Agent name: Codex
- Task requested: Execute the attached Archscry Phase 2 product-contract goal from accepted baseline `e875c624d2c3463aa6af9b8ab473c7401a4d6d14`, produce only the three scoped product artifacts, complete RobDev self-review, and obtain fresh independent RobQA.
- Related work: VM-551 Gate B1, VM-579, VM-586, VM-587, VM-588.
- Product-artifact status: `PASS — RobDev Ready`; independent RobQA pending.

## Phase 3 Purpose

Phase 3 must determine whether an experienced-player journey can save questions after a prior-independent observation floor without reducing unexpected-identity discoverability, weakening current evidence obligations, or turning self-report into placement authority.

Phase 3 is not authorized by this handoff. It requires a separate card and owner approval.

## Smallest Proof

Compare two version-bound paths using the current Gate B1 instrument and current result owner:

1. **Control:** the current route remains entirely prior-blind.
2. **Assisted shadow:** the current fixed Gate sequence remains prior-blind; only after the candidate blind floor may a prior choose between question opportunities already equally legitimate under behavioral evidence.

The assisted path must use the same question bank, mappings, candidate set, ranking, qualification, public-state normalization, stopping rules, and maximum question count. The prior cannot satisfy evidence or stopping. If current machinery cannot express that comparison without changing those contracts, Phase 3 stops.

The current four fixed Gate observations are the earliest repository-grounded candidate checkpoint to test. They are not pre-approved as a sufficient floor.

## Proof Claims And Evidence

| Claim Phase 3 must prove | Smallest evidence | Pass condition | Fail / fallback |
| --- | --- | --- | --- |
| Blind baseline | Instrumented trace shows no prior read or derived prior field before the tested checkpoint | Byte/field-level prior absence from candidate, ranking, qualification, routing, and stopping inputs before the floor | Keep the full route prior-blind; do not shorten |
| Narrow influence | Paired traces show the prior only breaks a genuine behavioral tie between eligible question opportunities | No candidate, utility class, evidence obligation, or stopping fact changes because of prior | Remove prior-assisted routing and retain prior for explanation only |
| Question-count savings | Paired route report records control and assisted question counts plus the exact behavioral obligations satisfied | A predeclared owner-approved material-savings threshold is met without changing the public result or evidence obligations | No production value: retain current route |
| Unexpected-identity discoverability | Adversarial matching, disjoint, multiple-prior, mechanic-only, Yore, close, and insufficient cases; include deliberately wrong priors against current deterministic witnesses | Assisted paths preserve every responsible unexpected named/unnamed outcome required by the control and never prune the unexpected direction | Any suppressed or prior-confirmed result fails the experiment |
| Fallback integrity | Forced unavailable/unsafe prior, no equal opportunity, stale prior, and version mismatch cases | The route falls back to current prior-blind behavior; completed readings remain snapshot-correct | Reject the assisted version; preserve the current instrument |

The numeric threshold for “material savings” is a Phase 3 product decision and must be declared before collection. Phase 2 does not invent it or retroactively choose one after seeing results.

## Reuse Plan

Phase 3 should extend, in order:

- the current Gate B1 pure engine and route traces;
- VM-579 Engine Validation for visible inspection;
- VM-586 current witnesses and result-state coverage;
- the 12 [Phase 2 contract fixtures](../contracts/archscry-phase-2-contract-fixtures.md);
- existing focused telemetry test sinks or a separately authorized shadow dataset.

Do not build a new questionnaire, recommendation engine, witness framework, persistence system, analytics provider, or browser harness.

Current production telemetry is not sufficient for the proof and must not be expanded implicitly. Any participant/shadow collection requires separate authorization for schema, consent, retention, and privacy. Prior prose and identity/mechanic claims must not enter the existing telemetry events.

## Phase 3 Stop Conditions

Stop with `BLOCKED` or `FAIL` if:

- the prior must enter ranking, qualification, candidate membership, or stopping to produce savings;
- the current fixed Gate checkpoint cannot be shown to be a safe blind floor;
- an unexpected identity becomes undiscoverable or less responsibly explained;
- a Yore prior produces a named Yore behavioral result;
- the experiment needs new mappings, questions, a composite score, or a new recommendation engine;
- the comparison cannot bind prior, observed result, and reconciliation to the same reading/version;
- participant data would be collected without separately approved consent and retention rules.

## Phase 3 Exit

Phase 3 may recommend a versioned implementation candidate only when all proof claims pass, fallbacks are deterministic, the savings threshold was predeclared, and no current placement or reading-state contract was weakened. It cannot itself authorize production cutover.

## Phase 2 Files Reviewed

- Repository-local RobDev and RobQA skills and their frozen authorities.
- Phase 1 acceptance/freeze, behavioral-model roadmap, VM-579/VM-586/VM-587 cards and recent handoffs.
- Gate B1 final architecture, identity-lens contract, instrument README, player-validation plan, and versioning plan.
- Current Gate B1 engine, Gate A public-state presenter, questionnaire, reading/dossier composer, session/profile persistence, and telemetry owner.
- Commander Compass, Taste Profile/returning-player backlog, dossier-to-Maze, Loom, route/data-flow, fixtures, witnesses, and browser QA seams.
- Current board, handoff index, branch/worktree state, and unrelated VM-578 untracked corpus boundary.

## Phase 2 Files Changed

- `docs/contracts/archscry-product-contract-v1.md`
- `docs/contracts/archscry-phase-2-contract-fixtures.md`
- `docs/handoffs/2026-08-23-1344-codex-archscry-phase-3-proof-handoff.md`
- `docs/kanban/in-progress/VM-588-archscry-phase-2-product-contract.md`
- `docs/kanban/board.md`
- `docs/handoffs/HANDOFF_INDEX.md` at closeout

## What Changed / Why

Phase 2 now has one reuse-first normative contract, one compact 12-case fixture suite, and this measurable Phase 3 proof boundary. The separation is necessary because the current repository owns behavior-first placement and downstream exploration but has no accepted starting-prior/reconciliation contract. The new semantics add no runtime layer.

## Decisions Made

- `self_reported_prior` and `observed_gameplay_fit` remain separate facts permanently.
- Reconciliation is deterministic set comparison and explanation only.
- Existing `IDENTITY_LENS_SELF_REPORT` remains a separate post-behavior lens.
- Mechanic/playstyle preferences may open approved exploration, never placement.
- Exploration uses categorical reason classes and no aggregate relevance score.
- Current fixed Gate observations are only the earliest Phase 3 floor candidate, not an accepted shortened path.
- Completed readings own immutable prior, observed-result, and reconciliation snapshots.

## RobDev Compact Packet

- Owning authority: owner attachment, Phase 1 acceptance, VM-588, and the normative product contract.
- Authoritative producer: authored documentation only.
- Changed behavior: repository product semantics and future implementation/proof boundaries.
- Protected behavior: all current UI, questionnaire, mappings, scoring, candidates, qualification, stopping, results, dossiers, Yore evidence, telemetry, persistence implementation, and VM-578.
- Reused machinery: Gate B1/Gate A, current result envelope, Commander Compass/dossier/Maze/Loom owners, VM-579/VM-586 fixtures and QA seams.
- Consumers: future Phase 3 experiment, result/explanation implementation, state-versioning work, and owner review.
- Smallest complete change: the three requested product artifacts plus required Kanban/index governance.
- Non-goals: UI, runtime, schema, engine, recommendation machinery, exact UI count, exact shortened count, experiment execution, recruitment, deployment.
- Stop conditions: any protected product mutation, unsupported mapping, new subsystem, hidden composite result, or stale reading attribution.

## Risks / Uncertainties

- Phase 3 still must prove whether the current four fixed Gate observations are a sufficient blind floor.
- “Material” question savings is a product threshold that must be predeclared in Phase 3.
- Current latest-only persistence does not itself provide reading history; future implementation must preserve snapshot semantics without pretending history exists.
- Approved mechanic-to-exploration relationships may be incomplete; absence must remain an honest omission rather than invented mapping.

## Tests Run

- Baseline/ref/worktree/status check: PASS — `HEAD`, local `main`, and `origin/main` started at exact accepted SHA `e875c624d2c3463aa6af9b8ab473c7401a4d6d14`, ahead/behind `0/0`, one worktree, no tracked dirt, and only the preserved VM-578 corpus untracked.
- Reuse-first source review: PASS — current Gate B1/Gate A, lens, result/persistence, telemetry, Commander Compass, dossier/Maze/Loom, fixture/witness, and roadmap owners are named; no parallel runtime owner was added.
- Fixture structure check: PASS — exactly 12 unique contiguous `P2-01` through `P2-12` rows and all five reconciliation states are present.
- Required contract-vocabulary/invariant check: PASS — all five exploration reasons, no-score/no-confidence rules, prior isolation, shortened-path deferral, and retake immutability are present.
- Relative Markdown link check: PASS — every relative link in the three artifacts and VM-588 card resolves.
- Changed-path allowlist: PASS — only VM-588 documentation/governance paths plus the untouched VM-578 boundary are present.
- `git diff --check`: PASS; only the existing board line-ending warning is reported.
- Manual RobDev red team: PASS — all 12 fixtures were read against the independent-review checklist; no prior scoring, qualification, pruning, Yore forcing, mechanic-to-placement authority, multiple-prior winner, insufficient suppression, historical mutation, stale retake attribution, or duplicate exploration owner remains.

QA classification: `QA-0` documentation/product-contract change. CPU-heavy, placement, browser, journey, synthetic, mutation, and recovery suites were intentionally not run because no runtime, UI, questionnaire, model, mapping, scoring, qualification, stopping, persistence, telemetry, or generated product artifact changed.

## RobDev Self-Review Disposition

`PASS — RobDev Ready`

The three artifacts are the smallest complete Phase 2 product slice. Every new semantic concept is necessary to distinguish player-authored context from current behavioral truth; all runtime/state/exploration machinery is reused or explicitly deferred. No genuine stop condition was reached.

## Not Touched

- No UI, runtime JavaScript, CSS, questionnaire, placement source/model, mapping, witness, dossier, Commander Compass data, Maze/Loom code, telemetry, persistence/schema, migration, deployment, or Phase 3 experiment.
- No VM-578/player-language corpus file was read, modified, staged, moved, or incorporated.

## Follow-Up Recommendation

If Phase 2 receives owner acceptance, create one bounded Phase 3 proof card from the proof table above. Do not implement a shortened path before that proof passes.

## Next Suggested Agent

Fresh independent RobQA reviewer for the exact Phase 2 documentation candidate.

## Independent RobQA

Status: `PENDING`

The fresh reviewer must append exact candidate binding, QA classification, selected checks, findings, skipped suites, and one disposition:

- `PASS — Owner Review Ready`
- `FAIL — Return to RobDev`
- `BLOCKED`
