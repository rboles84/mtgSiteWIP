# CDN And Font Dependency Review

VM-146 reviews public-route runtime dependencies without changing route behavior. It separates external asset delivery from product/service behavior, and it treats legal-policy text as disclosure copy rather than proof of a live browser dependency.

## Scope

- Public routes reviewed: `/`, `/archscry/`, `/maze/`, `/strategium/`, `/apocrypha/`, `/library/`, `/privacy/`, and `/terms/`.
- Inputs reviewed: live route HTML plus only the CSS and JS files directly loaded by those routes.
- `research/**` files were included only where they are directly loaded by a public route, currently Maze through `maze/index.html` -> `research/research-init.js`.
- Local vendored code, such as `assets/js/graph.js`, is not counted as a CDN dependency even when the upstream package originated elsewhere.
- Google Fonts is counted as `fonts.googleapis.com` stylesheet delivery plus implied `fonts.gstatic.com` font-file delivery.

## Recommendation Key

| Recommendation | Meaning |
|---|---|
| Keep | Preserve the dependency or service behavior in current product scope. |
| Replace | Safe candidate for a later implementation card to remove or replace the external delivery path, with visual/session verification. |
| Defer | Do not remove in this review; keep analysis documented and handle with a later scoped implementation card. |

## Route Review

| Route | Boot-Time External Assets | Interactive / Runtime External Sources | Copy-Only Mentions | Ownership Note | Recommendation | Rationale | Evidence Note |
|---|---|---|---|---|---|---|---|
| `/` | `fonts.googleapis.com` route-owned stylesheet for Cinzel; implied `fonts.gstatic.com` font files. | None found in route-loaded JS. `assets/js/graph.js` is local vendored code, not CDN-loaded. | None. | Direct route-owned font load in `index.html`; local shared CSS also loads self-hosted fonts through `assets/css/fonts.css`. | Google Fonts: defer. | Home still depends on hardcoded Cinzel usage in markup, CSS, and chart/canvas labels. Removing the Google stylesheet would be visual work, not a docs-only review. | `index.html:17`, `index.html:692`, `assets/css/home.css:45`, `assets/css/home.css:1763`, `assets/js/home.js:319`, `assets/js/home.js:718`. |
| `/archscry/` | `cdn.jsdelivr.net` Supabase UMD asset; `fonts.googleapis.com` route-owned stylesheet for Cinzel Decorative, Cinzel, Crimson Pro, and IBM Plex Mono; implied `fonts.gstatic.com` font files. | Supabase project/API through `assets/js/shared.js`; Scryfall named-card API for card images; Scryfall web links; EDHREC, MTGDecks, Archidekt, Moxfield, and Google fallback search links from dossier flows. | Google sign-in appears in route copy as user-facing save language. | Supabase CDN asset is route-owned by the head script. Supabase service behavior is inherited through shared session code and is a separate product dependency. Commander-directory links are route behavior, not boot assets. | Google Fonts: defer. Supabase CDN asset: defer. Supabase-backed behavior: keep. Scryfall and Commander-directory services: keep. | Archscry has direct font-family references in route CSS and depends on Supabase-backed save/session behavior. The CDN delivery mechanism may be reviewed later, but this card must not remove saved-result behavior. Outbound deck/card links are core dossier affordances. | `archscry/index.html:7`, `archscry/index.html:10`, `archscry/index.html:11`, `assets/js/shared.js:7`, `assets/js/shared.js:32`, `assets/js/shared.js:554`, `assets/js/index.js:1163`, `assets/js/index.js:1164`, `assets/js/index.js:1165`, `assets/js/index.js:1179`, `assets/js/index.js:1241`, `assets/js/index.js:2540`, `assets/css/archscry.css:1185`, `assets/css/archscry.css:1323`, `assets/css/archscry.css:2464`. |
| `/maze/` | `cdn.jsdelivr.net` Supabase UMD asset; `fonts.googleapis.com` route-owned stylesheet for Cinzel, Crimson Pro, and IBM Plex Mono; implied `fonts.gstatic.com` font files. | Supabase project/API through `assets/js/shared.js`; Scryfall API search, named-card, and random-card calls through route-loaded `research` modules; Scryfall web links; TCGPlayer product search links. | Scryfall is also mentioned in visible help copy. | Supabase CDN asset is route-owned by the head script. Supabase service behavior is inherited through shared session code. `research/research-init.js`, `research/research-search.js`, and `research/research-ui.js` are live because Maze loads `research/research-init.js` as a module. | Google Fonts: defer. Supabase CDN asset: defer. Supabase-backed behavior: keep. Scryfall and TCGPlayer flows: keep. | Maze still has direct Cinzel/Crimson/Plex font expectations in route CSS and live research UI. Scryfall API access is the core search behavior. TCGPlayer and Scryfall web links are user-triggered card-detail affordances. | `maze/index.html:7`, `maze/index.html:9`, `maze/index.html:10`, `maze/index.html:328`, `assets/js/shared.js:7`, `assets/js/shared.js:32`, `research/research-init.js:6`, `research/research-init.js:7`, `research/research-search.js:40`, `research/research-search.js:55`, `research/research-search.js:81`, `research/research-ui.js:87`, `research/research-init.js:960`, `research/research-init.js:2000`, `assets/css/maze.css:34`, `assets/css/maze.css:470`, `research/research-init.js:2476`. |
| `/strategium/` | `fonts.googleapis.com` route-owned stylesheet for Cinzel; implied `fonts.gstatic.com` font files. | None found in route-loaded JS. | None. | Direct route-owned font load in `strategium/index.html`; shared topbar/reduce-motion code is local and does not add a third-party runtime dependency. | Google Fonts: defer. | Strategium still hardcodes Cinzel in its route stylesheet, so removing the Google stylesheet is visual work that needs a later scoped card and visual QA. | `strategium/index.html:7`, `assets/css/strategium.css:37`. |
| `/apocrypha/` | `fonts.googleapis.com` route-owned stylesheet for Cinzel, Cinzel Decorative, Crimson Pro, and IBM Plex Mono; implied `fonts.gstatic.com` font files. | Outbound public reference links may navigate away, but no route-loaded JS fetches a third-party service at runtime. | None. | The Google import is route-head owned. The route and shared shell styles found in scope use token fonts such as `var(--font-display)`, `var(--font-text)`, and `var(--font-mono)`, backed by local `assets/fonts` through `assets/css/fonts.css`. | Google Fonts: replace candidate. Outbound public reference links: keep. | The route appears to rely on token-based local fonts rather than direct Google-family declarations in live CSS/JS. A later implementation card can remove the Google import only with visual compatibility checks. | `apocrypha/index.html:11`, `apocrypha/index.html:12`, `assets/css/apocrypha.css:23`, `assets/css/apocrypha.css:164`, `assets/css/apocrypha.css:1180`, `assets/css/fonts.css:18`, `assets/css/fonts.css:47`, `assets/css/fonts.css:74`. |
| `/library/` | `fonts.googleapis.com` route-owned stylesheet for Cinzel and Crimson Pro; implied `fonts.gstatic.com` font files. | Meta refresh and inline redirect to local `../apocrypha/`; no third-party runtime service. | None. | This is a redirect shell. Any meaningful typography comes from shared shell/token inheritance and local inline alias styles using token fonts; the Google import is route-head owned but not meaningful route behavior. | Google Fonts: replace candidate, limited to shared-shell visual compatibility. Local alias redirect: keep. | The route has no substantial route-owned font dependency beyond inherited shared shell presentation. Removing the import should be handled later with a quick alias visual/redirect check. | `library/index.html:7`, `library/index.html:9`, `library/index.html:10`, `library/index.html:22`, `library/index.html:40`. |
| `/privacy/` | None found. Route loads local CSS/JS only. | None found. | Google, Supabase, Anthropic, and Scryfall appear in policy copy as service disclosures. | Shared shell dependency only: local `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `legal.css`, `reduce-motion.js`, and `vm-topbar.js`. No route-owned third-party asset load. | Runtime third-party dependencies: keep absent. Policy-copy mentions: keep. | Legal copy should continue disclosing relevant providers, but grep hits in body text are not runtime dependencies. | `privacy/index.html:8` through `privacy/index.html:14`, `privacy/index.html:100`, `privacy/index.html:156`, `privacy/index.html:157`, `privacy/index.html:205`, `privacy/index.html:206`. |
| `/terms/` | None found. Route loads local CSS/JS only. | None found. | Google appears in optional sign-in copy; third-party providers are mentioned as service/availability terms. | Shared shell dependency only: local `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `legal.css`, `reduce-motion.js`, and `vm-topbar.js`. No route-owned third-party asset load. | Runtime third-party dependencies: keep absent. Policy-copy mentions: keep. | Terms copy should stay aligned with product disclosures, but body-copy references are not browser runtime dependencies. | `terms/index.html:8` through `terms/index.html:14`, `terms/index.html:83`, `terms/index.html:135`, `terms/index.html:204`, `terms/index.html:205`. |

## Cross-Route Findings

- Supabase must be split into two findings: the CDN asset on Archscry and Maze, and the Supabase-backed auth/session/database behavior inside `assets/js/shared.js`.
- Google Fonts removal is not one global operation. Home, Archscry, Maze, and Strategium still have direct route-owned font-family expectations, while Apocrypha and Library look like safer replacement candidates because their live styling leans on shared token fonts.
- Privacy and Terms should remain excluded from runtime dependency counts unless their head scripts/styles change. Their third-party names are legal disclosures, not live service calls.
- Shared shell CSS/JS currently contributes local dependencies to all public routes, but it does not itself fetch Google Fonts, Supabase, Scryfall, or other third-party services.

## Follow-Up Recommendations

- Open a later scoped implementation card for Google Fonts replacement candidates only after deciding whether to remove redundant route-head imports route by route.
- Treat Supabase CDN replacement as a delivery-mechanism card, not an auth/session removal card.
- Any future dependency review should repeat the route-loaded-file boundary used here so archived preview files and test fixtures do not pollute the live dependency inventory.
