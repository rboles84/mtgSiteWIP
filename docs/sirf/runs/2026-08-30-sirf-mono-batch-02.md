# SIRF Mono Batch 02 — 2026-08-30

## Manifest

- Control card: VM-600.
- Branch: `main`.
- Starting HEAD / `origin/main`: `6d2d2b739b7c8a69f9aed134eda259f385d76c10`.
- Starting divergence: `0/0`.
- Identities: Blue (`U`), Black (`B`), Red (`R`), and Green (`G`) only.
- Protected controls: WUBRG, Temur, Lorehold, White, Rakdos, and Esper.
- Preserved unrelated untracked paths: `docs/incidents/recoveries/VM-596-wubrg-semantic-repair-control.md`, `docs/research/maze-player-language/corpus/`, and `outputs/01a02cd6-bce7-7832-9558-3075c52f146a/archscry-current-state-2026-08-30/`.

## Pre-flight disposition

Proceed with one exception-based batch. Certified raw mono claims and profiles are coherent; the defects are downstream, their owners are known, and no external verification is required. Review level is high semantic QA with QA-1 rendered-product validation. Stop conditions are the SIRF P0/P1, ownership, accepted-control, rendered-proof, freshness, and three-cycle rules.

## Cycle 1

The batch found the same four owned defect families in each identity:

1. What to Look For cited generated `data/factions.json` as evidence rather than certified raw claims (P0 provenance).
2. Start Here mixed generic fallback directions with the curated What to Look For taxonomy (P2 rendered-contract fidelity).
3. Start Here repeated the How This Plays mechanics/game-plan inventory rather than supporting a construction choice (P2 section ownership).
4. The authored boundary rendered `when if` (P3 grammar).

Repairs rebound the twelve lane entries to the certified mono mechanical claims, added each accepted three-lane set to the existing `starterDirections` owner, changed Start Here spellcraft into lane-choice guidance, repaired the four authored boundaries, recalculated source copy hashes, and regenerated the dossier catalog through its existing builder.

## Convergence

- Blue: one cycle; all gates PASS.
- Black: one cycle; all gates PASS.
- Red: one cycle; all gates PASS.
- Green: one cycle; all gates PASS.
- P0 `4 found / 4 fixed / 0 open`.
- P1 `0 found / 0 fixed / 0 open`.
- P2 `8 found / 8 fixed / 0 open`.
- P3 `4 found / 4 fixed / 0 open`.
- Shared implementation: the already-established curated-directions branch in the common Start Here composer was reused. No composer, recommender, filter, renderer, or precon source defect remained, so no shared runtime code change was needed.
- External sources consulted: none; governed raw claims and committed product data were sufficient and non-conflicting.

## Rendered evidence

The actual `/archscry/?vm-dev-review=1&reviewIdentity=<key>` route was reviewed at the browser default desktop viewport and at a 375 CSS-pixel content width. All four render the five scoped sections, have no horizontal overflow, show the accepted taxonomies in order, omit Native, order Exact before Stretch, and give each precon a recorded game-plan rationale.

The mandatory section-role matrices, final taxonomies, recommendation results, VM-595 delta, findings, tests, and exact candidate manifest are recorded in `docs/sirf/reports/2026-08-30-sirf-mono-batch-02.md`.

## Disposition

`ACCEPTED_EXCEPTION_BATCH` — no defined SIRF stop condition remains. Promote all four contracts, close VM-600, commit as one independently attributable batch, push normally to `origin/main`, and verify divergence returns to `0/0`.
