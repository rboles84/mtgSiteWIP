# SIRF Guild Batch 03 — 2026-08-30

## Manifest

- Control card: VM-601.
- Branch: `main`.
- Starting HEAD / `origin/main`: `c2315ae3fc9ebced488ea1ddd42a709dd96eb0e7`.
- Starting divergence: `0/0`.
- Identities: Azorius (`WU`), Boros (`WR`), Orzhov (`WB`), and Simic (`UG`) only.
- Selection basis: Boros supplied the highest semantic/review risk, Orzhov the strong-voice control, Azorius the procedure/timing boundary, and Simic the largest unsupported Native group.
- Protected controls: WUBRG, Temur, Lorehold, White, Rakdos, Esper, Blue, Black, Red, and Green.
- Preserved unrelated untracked paths: `docs/incidents/recoveries/VM-596-wubrg-semantic-repair-control.md`, `docs/research/maze-player-language/corpus/`, and `outputs/01a02cd6-bce7-7832-9558-3075c52f146a/archscry-current-state-2026-08-30/`.

## Pre-flight disposition

Proceed with one high-risk semantic/provenance cycle. Certified raw guild claims, profiles, identity/metaphysics records, and committed product facts were coherent. The observed defects were downstream with known owners, so no external verification was materially required. Placement, scoring, qualification, routing, and CRIT-001 certification truth remained protected.

## Cycle 1

Each identity exposed the same five owned defect families:

1. What to Look For cited generated `data/factions.json` as evidence (P0 provenance).
2. Generic same-color products were promoted to Native through unsupported `factionRefs` (P1 relationship truth).
3. Start Here omitted the third curated lane and appended generic fallback taxonomy (P2 rendered-contract fidelity).
4. Guild Spellcraft repeated How This Plays' mechanics inventory (P2 section ownership).
5. Test the Fit used the difficult `when X is not Y` boundary construction (P3 grammar).

The repair rebound twelve curated lanes to certified raw claims, installed the exact three-lane sets in the existing guidance owner, made Guild Spellcraft a construction choice, recast the four boundaries, and removed sixteen unsupported guild relationships in the canonical precon source. First Flight remained Azorius Native because Isperia's guild leadership is explicitly governed. Existing builders regenerated both catalogs.

## Convergence

- Azorius: one cycle; all gates PASS.
- Boros: one cycle; all gates PASS.
- Orzhov: one cycle; all gates PASS.
- Simic: one cycle; all gates PASS.
- P0 `4 found / 4 fixed / 0 open`.
- P1 `4 found / 4 fixed / 0 open` across sixteen unsupported product relationships.
- P2 `8 found / 8 fixed / 0 open`.
- P3 `4 found / 4 fixed / 0 open`.
- Shared repair: the canonical Commander guidance and precon relationship owners were repaired once; no identity-specific renderer, classifier, or fallback workaround was added.
- External sources consulted: none; governed repository evidence was sufficient and non-conflicting.

## Rendered evidence

The actual `/archscry/?vm-dev-review=1&reviewIdentity=<key>` Dossier Review path was reviewed at 1280×720 and 375×812. All four render the five scoped sections with no page or section overflow, exact matching taxonomies, distinct section responsibilities, and Native → Exact → Stretch or Exact → Stretch ordering. The six required controls were rerendered at both widths with no overflow or taxonomy/relationship regression.

The matrices, exact taxonomies, product relationships, scoped VM-595 delta, tests, known baseline fixture issue, and exact candidate manifest are recorded in `docs/sirf/reports/2026-08-30-sirf-guild-batch-03.md`.

## Disposition

`ACCEPTED_EXCEPTION_BATCH` — no defined SIRF stop condition remains. Promote the four contracts, close VM-601, commit and push normally, verify `0/0`, then open Guild Batch 04 at the new accepted SHA.
